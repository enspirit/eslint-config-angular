# Eslint config for Enspirit's angular projects

## How to use

Install the plugin:
```
npm install --save-dev @enspirit/eslint-config-angular
```

Edit your .eslintrc.js to include the plugin:

```
module.exports = {
  'extends': '@enspirit/eslint-config-angular'
};
```
